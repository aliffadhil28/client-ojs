// src/setupTests.js
import { setGlobalConfig } from 'vitest';

// Set global configurations for Vitest
setGlobalConfig({
  // Any global configurations you want to set, such as base URL for API requests, etc.
});

// Optionally, you can add any setup or teardown logic here that should run before/after each test file
